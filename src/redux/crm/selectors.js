export const selectLoading = (state) => state.crm.isLoading;

export const selectError = (state) => state.crm.error;

export const selectRecords = (state) => state.crm.records;

export const selectServiceData = (state) => state.crm.serviceData;

export const selectVisits = (state) => state.crm.visits;