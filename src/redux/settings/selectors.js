export const selectIsLoading = (state) => state.settings.isLoading;

export const selectError = (state) => state.settings.error;

export const selectSchedule = (state) => state.settings.schedule;

export const selectPosts = (state) => state.settings.posts;

export const selectPrices = (state) => state.settings.prices;

export const selectEmployees = (state) => state.settings.employees;
