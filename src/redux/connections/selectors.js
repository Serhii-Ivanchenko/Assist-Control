export const selectLoading = (state) => state.connections.isLoading;

export const selectError = (state) => state.connections.error;

export const selectStats = (state) => state.connections.stats;

export const selectConnectionsList = (state) => state.connections.connectionsList;
